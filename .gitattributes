*.ipynb linguist-vendored

# force *nix line endings so files don't look modified in container run from Windows clone
* text eol=lf
# Git incorrectly thinks some media is text
*.png -text
*.mp4 -text
# >>> CUSTOM: HiClaw — binary files must not have line-ending conversion <<<
*.whl binary
*.bundle binary
*.tar.gz binary
*.gz binary
*.zip binary
# >>> END CUSTOM <<<
