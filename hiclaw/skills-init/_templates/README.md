# HiClaw Skills 模板

创建新 skill 时，复制对应模板：
- `SKILL-template.md` → 单步技能
- `workflow-template.md` → 多步编排流程

## 目录规范
- 每个 skill 一个文件夹，文件夹名即 skill 名
- `SKILL.md` 是唯一入口（必须）
- `scripts/` 放可执行脚本（可选）
- `references/` 放参考资料（可选）
- `_` 开头的目录不会被加载为 skill
